---
import Redirect from '../../components/Redirect.astro';
import { defaultBookVersion } from '../../data/versions';

const target = `${import.meta.env.BASE_URL}pt-br/${defaultBookVersion.slug}/`;
---

<Redirect {target} />
